import React from 'react';
import { Card } from '../../components/ui/weather-card/weather-card.styled';

const WeatherAllCard: React.FC = () => {
  return (
    <Card>
      <div></div>
    </Card>
  );
};

export default WeatherAllCard;
